<!-- Hero Section for Apparel Store -->
<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-gray-50 to-gray-100">
  <!-- Background pattern/overlay -->
  <div
    class="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1558769132-cb1c458e4222?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2000&q=80')] bg-cover bg-center opacity-10"
  ></div>

  <!-- Content container -->
  <div class="container mx-auto px-4 md:px-8 relative z-10">
    <div class="flex flex-col lg:flex-row items-center justify-between gap-12">
      <!-- Text content -->
      <div class="lg:w-1/2 text-center lg:text-left animate-fade-in">
        <h1 class="hero-title text-5xl md:text-6xl lg:text-7xl font-bold text-gray-900 mb-6">
          Elevate Your <span class="text-indigo-600">Everyday</span> Style
        </h1>

        <p class="text-gray-600 text-lg md:text-xl mb-10 max-w-2xl">
          Discover premium apparel crafted for comfort and designed for life's moments. From casual essentials to
          statement pieces, find your perfect fit.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
          <a
            href="#"
            class="btn-hover bg-gray-900 text-white px-8 py-4 rounded-full font-semibold text-lg flex items-center justify-center gap-2 hover:bg-gray-800 hover:shadow-xl transition-all duration-300"
          >
            Shop Collection
            <i class="fas fa-arrow-right text-sm"></i>
          </a>

          <a
            href="#"
            class="btn-hover border-2 border-gray-800 text-gray-900 px-8 py-4 rounded-full font-semibold text-lg flex items-center justify-center gap-2 hover:bg-gray-800 hover:text-white transition-all duration-300"
          >
            <i class="fas fa-play"></i>
            Lookbook
          </a>
        </div>

        <div class="mt-12 flex flex-wrap items-center justify-center lg:justify-start gap-8 text-gray-700">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-blue-100 flex items-center justify-center">
              <i class="fas fa-truck text-blue-600"></i>
            </div>
            <span class="font-medium">Free Shipping</span>
          </div>
        </div>
      </div>

      <!-- Image/Product showcase -->
      <div class="lg:w-1/2 relative">
        <div class="relative">
          <!-- Main product image -->
          <div class="relative rounded-3xl overflow-hidden shadow-2xl transform rotate-3 hover:rotate-0 transition-transform duration-700">
            <img
              src="https://images.unsplash.com/photo-1490481651871-ab68de25d43d?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1200&q=80"
              alt="Stylish apparel collection"
              class="w-full h-auto object-cover"
              width="1200"
              height="800"
            >
            <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
          </div>

          <!-- Floating badge -->
          <div class="absolute -top-4 -right-4 bg-white rounded-full shadow-2xl p-4 animate-pulse">
            <div class="text-center">
              <div class="text-2xl font-bold text-gray-900">30%</div>
              <div class="text-xs text-gray-600">OFF</div>
            </div>
          </div>

          <!-- Secondary image overlay -->
          <div class="absolute -bottom-6 -left-6 w-48 h-48 rounded-2xl overflow-hidden shadow-xl transform -rotate-12 border-8 border-white hidden md:block">
            <img
              src="https://images.unsplash.com/photo-1523381210434-271e8be1f52b?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80"
              alt="Casual apparel"
              class="w-full h-full object-cover"
              width="600"
              height="600"
            >
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2">
    <a href="#next" class="flex flex-col items-center text-gray-700 hover:text-gray-900 transition-colors">
      <span class="text-sm font-medium mb-2">Explore More</span>
      <i class="fas fa-chevron-down animate-bounce"></i>
    </a>
  </div>
</section>

<style>
  /* Custom animations */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 1s ease-out forwards;
  }

  .btn-hover {
    transition: all 0.3s ease;
  }

  /* Font imports - add to your theme's CSS */
  /* @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap'); */
  /* 
    For Shopify, you would add the font import in your theme.liquid file
    and set:
    .hero-title { font-family: 'Playfair Display', serif; }
    body { font-family: 'Montserrat', sans-serif; }
  */
</style>
